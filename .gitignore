# packwiz cache
.packwizcache/

# Claude Code local session state (not project content)
.claude/

# Secrets / local overrides for docker-compose
.env

# Build outputs del mod (updater/)
updater/build/
updater/.gradle/

# OS/editor cruft
.DS_Store
*.swp

# Note: server runtime (world, mods, logs, config) lives entirely outside
# this repo, in ~/deployments/minecraft/mainMinecraftServer/ - see README.
updater/run/
